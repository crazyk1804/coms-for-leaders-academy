package prs.crazyk.cmm.repository;

import org.springframework.stereotype.Repository;

import java.util.List;

@Repository("CMMRepository")
public interface CMMRepository {
//    List<SystemCode> selectSystemCode(AISSConditions conditions);

//    long selectSystemUserCount(AISSConditions conditions);
//    List<SystemUser> selectSystemUsers(AISSConditions conditions);
//
//    SystemUser selectSystemUser(AISSConditions conditions);
//    int insertSystemUser(SystemUser user);
//    int updateSystemUser(SystemUser user);
//    int deleteSystemUser(SystemUser user);

//    List<UserGroup> selectUserGroups();
}
